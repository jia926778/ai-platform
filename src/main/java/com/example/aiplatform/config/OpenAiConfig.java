package com.example.aiplatform.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class OpenAiConfig {
    // Spring AI auto-configuration handles OpenAI client setup via application.yml.
    // Add custom beans here if additional configuration is needed.
}
